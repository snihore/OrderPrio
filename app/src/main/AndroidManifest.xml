<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.orderprio">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_final"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_final"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CustomerPaymentActivity"></activity>
        <activity android:name=".ShopPaymentsActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".ScanQRActivity" />
        <activity android:name=".CustomerDashboard" />
        <activity android:name=".ShopQRActivity" />
        <activity android:name=".AddMenuActivity" />
        <activity android:name=".ShopDashboard" />
        <activity android:name=".ShopRegistration" />
        <activity android:name=".OptionActivity" />
        <activity android:name=".EnterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>